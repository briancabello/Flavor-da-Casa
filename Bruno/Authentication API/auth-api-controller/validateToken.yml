info:
  name: validateToken
  type: http
  seq: 1
  tags:
    - auth-api-controller

http:
  method: POST
  url: "{{baseUrl}}/api/auth/validate"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/auth/validate"
      method: POST
      body:
        type: json
        data: "{}"
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "authenticated": false,
            "invalid": false,
            "successful": false,
            "validationErrors": [
              {
                "field": "",
                "message": "",
                "value": ""
              }
            ],
            "value": {
              "id": 0,
              "email": "",
              "userName": "",
              "empty": false
            },
            "empty": false,
            "error": false
          }
